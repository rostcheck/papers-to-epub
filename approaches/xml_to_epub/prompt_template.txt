Please read the LaTeX file '{latex_file}' and convert it to XML format that complies with the provided academic paper schema.

REQUIREMENTS:
1. Read and parse the entire LaTeX file
2. Extract title, authors (with affiliations and emails), abstract, all sections and subsections
3. Convert LaTeX math expressions to proper MathML format
4. Extract tables, figures, equations, references, and citations
5. Use XHTML formatting for text content (paragraphs, emphasis, etc.)
6. Create valid XML that validates against the schema
7. Write the complete XML to '{output_file}'

XML SCHEMA:
{schema_content}

EXAMPLE STRUCTURE:
{xml_sample}

Please create the complete XML file with all content from the LaTeX paper.
